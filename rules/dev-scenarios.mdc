---
description: 典型操作场景记录规范 — 有价值的操作过程自动输出到项目的 docs/dev-scenarios/
alwaysApply: true
---

# 典型场景日志记录规范

当完成一个操作后，判断是否属于「典型场景」，如果是，将完整过程输出为 Markdown 日志文件。

## 何时记录

符合以下任一条件即应记录到 `docs/dev-scenarios/`（若项目无此目录则创建）：

1. **首次操作**：该类型操作在项目中第一次执行（如首次配置环境、首次发布、首次搭建 CI）
2. **踩坑与修复**：过程中遇到了非显而易见的错误，并找到了解决方案（如版本兼容性、配置差异、平台差异）
3. **多步骤流程**：操作涉及 3 个以上步骤的完整工作流
4. **环境配置**：涉及开发环境、CI/CD、工具链等基础设施配置
5. **破坏性变更**：涉及 major 版本升级、依赖替换、架构调整等不可逆操作
6. **团队复用价值**：其他成员很可能会遇到同样的问题或需要执行同样的操作

## 不需要记录

- 日常的代码修改（改 bug、加样式、重构）
- 简单的单命令操作
- 重复已有场景的操作

## 日志格式

```markdown
# 标题

执行时间: YYYY-MM-DD HH:mm
涉及范围: xxx

## 场景说明
## 操作步骤（带完整命令和输出）
## 踩坑记录（如有）
## 验证
## 要点总结
```

## 注意事项

- 命令输出如实记录，包含 exit code
- 敏感信息用 `****` 或 `<占位符>` 脱敏
- 错误和重试过程保留，标注原因和修复方式
