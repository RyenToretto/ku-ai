---
description: 每次完成实质性工作后，在回复末尾给出"下一步行动卡"，方便用户直接开启新会话或继续当前任务。
globs:
alwaysApply: true
---

# 下一步行动卡规范

## 何时输出

满足以下任意一条即必须输出：

1. 本次回复**完成了一个或多个 Task**（如写文件、执行迁移、修复 Bug）
2. 本次回复**明确结束了某个阶段**（如 lint 通过、commit 完成、回归验证完毕）
3. 用户的问题或指令**涉及多步骤工作流**，本次只完成了部分步骤

以下情况**不需要输出**：

- 纯问答、解释类回复（不涉及文件改动）
- 只列计划但未动手的回复
- 用户明确说"不用给下一步"

## 输出格式

在回复**最末尾**（所有正文、代码块、commit hash 之后）追加以下卡片，使用 `---` 横线分隔：

```
---

## ⏭️ 下一步行动卡

**下一步**: <用 1-2 句话说清楚下一个最重要的任务是什么，以及为什么先做它>

**推荐模式**: <Agent 模式 | Plan 模式>
**推荐模型**: <Sonnet 4.6 | Opus 4.7 | GPT-5.5 | Composer 2.5>
**理由**: <一句话说明推荐该模型和模式的原因>

**可直接复制的 Prompt**:
\`\`\`
<完整 prompt，读者可以零编辑粘贴到新会话，包含：handoff 路径（若有）/ 具体任务指令 / 关键约束>
\`\`\`
```

## 注意事项

- **Prompt 必须可以零编辑直接粘贴**：不要用 `<填写XXX>` 或 `...` 占位符；所有变量（文件路径、Task ID 等）必须填写实际值
- **不要在正文中重复行动卡的内容**：行动卡是总结，不是正文的复述
- **模式选择标准**：
  - `Agent 模式`：任务明确、可执行，agent 直接动手写代码/运行命令
  - `Plan 模式`：任务模糊、有多条等价路径、需要用户确认方向
- **模型选择标准**（仅从以下 4 个可用模型中选择）：

  | 模型 | 适用场景 | 何时选它 |
  |------|----------|----------|
  | `Sonnet 4.6` | **日常主力**：多文件改动、CRUD、常规重构、前后端联调、中等复杂度 Bug | 绝大多数任务的默认选择，速度与质量最均衡 |
  | `Opus 4.7` | **复杂推理**：系统架构设计、ADR 决策、跨模块深度分析、长链路任务、疑难 Bug 溯源 | 任务涉及"系统级判断"或"超长推理链"时升级 |
  | `GPT-5.5` | **稳定执行**：规范文档输出、结构化代码生成、遵循指令一致性要求高的任务 | Claude 多次偏离指令或需要高格式一致性时备选 |
  | `Composer 2.5` | **轻量快速**：单文件小修改、格式修复、简单问答、低成本迭代 | 任务只是"小修小补"且不涉及复杂逻辑时使用 |
