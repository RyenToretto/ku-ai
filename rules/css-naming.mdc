---
description: CSS class / DOM id 命名约束 - 根 class 强制等于组件 kebab-case; 同一组件内任意两 class/id 互不为字符串子串 (铁律, 含 BEM element-of-element)
alwaysApply: true
---

# CSS / DOM 命名约束

## 核心规则

### 规则 1: 根 class 必须等于组件名的 kebab-case

组件文件名 `AaaaBbb.vue` → 根元素 class 必须是 `aaaa-bbb`, **不允许加任何后缀** (如 `-page` / `-host` / `-root`)。

```vue
<!-- OssUploader.vue -->
<template>
  <div class="oss-uploader">  <!-- ✅ 根 = 组件名 kebab-case -->
    ...
  </div>
</template>
```

```vue
<!-- ❌ 错误: 根加后缀 -->
<div class="oss-uploader-host">
<div class="oss-uploader-page">
```

### 规则 2: 同一组件内, 任意两个 class / id 必须互不为字符串子串

**铁律, 无任何弱化空间, 三种关系全部禁止**:

1. 根 class ↔ 任一子元素 class 不为子串
2. **任意两个子元素 class 之间不为子串** (旧规则曾用"应避免", 现升级为铁律)
3. **同一 BEM block 的多个 element 名之间也不为子串** (常见违规: `__cta` 与 `__cta-ico` / `__plan` 与 `__plan-name` / `__feature` 与 `__features`)

> "子串" 指字符串字面包含关系, **不依赖 `-` / `_` / `__` / `--` 分隔符的语义**。
> 即 `cta` 是 `cta-ico` / `cta_ico` / `ctaIco` / `cta--small` 的子串, 全部违规。

### 规则 3: 子元素 BEM block 用与根名不同的独立命名空间

由于规则 1 + 规则 2 的联合约束, 子元素**不能**继续以根 class 作 BEM block 名 (即不能写 `.oss-uploader__input`), 必须换成其他不与项目内任何 class 冲突的命名空间。

命名空间选择优先级:
1. **业务语义命名** (推荐): 用业务实体或操作动词, 如 `drama-ep__list` (短剧剧集列表)、`oss-pick__input` (OSS 选择器输入)
2. **功能简称**: 用组件功能的简短表达, 如 `creator-form__panel`、`tag-picker__chip`
3. **避免**: 纯缩写如 `ouv__xxx`、`elf__xxx` (语义模糊)

### 规则 4: BEM element 命名扁平化, 禁止 element-of-element

**`block__a` 与 `block__a-x` 同时存在 = 前缀子串违规 = 禁止**。

如需在某个 element 内部继续表达层级, 二选一:

- **首选**: 扁平化 element, 多个 element 平级共享 block 前缀, 名字彼此互不为子串
- **次选**: 用 BEM modifier `block__a--variant` 表达"是 a 的某种变体"

```vue
<!-- ❌ 违规: element-of-element 共享前缀 -->
<button class="blur-curtain__cta">
  <i class="blur-curtain__cta-ico" />     <!-- ❌ 'cta' 是 'cta-ico' 前缀子串 -->
  <span class="blur-curtain__cta-label" /><!-- ❌ 'cta' 是 'cta-label' 前缀子串 -->
</button>

<!-- ✅ 合规方案 A: 扁平化 element -->
<button class="blur-curtain__cta">
  <i class="blur-curtain__icon" />        <!-- ✅ 'cta' / 'icon' / 'label' 互不为子串 -->
  <span class="blur-curtain__label" />
</button>

<!-- ✅ 合规方案 B: 仅当多种 element 同形态时用 modifier -->
<button class="blur-curtain__cta blur-curtain__cta--primary">...</button>
<button class="blur-curtain__cta blur-curtain__cta--ghost">...</button>
```

#### 单复数陷阱

`__feature` (单个项) 与 `__features` (容器) 也是子串关系:

```vue
<!-- ❌ 违规 -->
<ul class="paywall-modal__features">
  <li class="paywall-modal__feature">...</li>  <!-- 'feature' 是 'features' 前缀子串 -->
</ul>

<!-- ✅ 改用不同名 -->
<ul class="paywall-modal__feature-list">
  <li class="paywall-modal__feature-item">...</li>  <!-- 'feature-list' / 'feature-item' 互不为子串 -->
</ul>
```

### 规则 5: BEM block 命名空间不应单独作为 class 使用

一个 BEM block 名(子元素命名空间)如果**既作为前缀**(`block__element`)**又作为独立 class**(`<div class="block">`), 必然与其 element 互为前缀子串, 触发规则 2:

```vue
<!-- ❌ 违规: topbar-home 既单独使用又作 element 前缀 -->
<button class="topbar-home">                 <!-- 单独 class -->
  <span class="topbar-home__ico" />          <!-- 'topbar-home' 是 'topbar-home__ico' 前缀子串 -->
</button>

<!-- ✅ 合规方案 A: 用 wrap element 包住, 不让 block 名单独出现 -->
<button class="topbar-home__btn">
  <span class="topbar-home__ico" />          <!-- 'btn' / 'ico' 互不为子串 -->
</button>

<!-- ✅ 合规方案 B: 把 block 单独使用语义改成独立 class (无 element 后缀) -->
<button class="home-btn">                     <!-- block 不再用作前缀 -->
  <span class="home-ico" />                   <!-- 平级独立 class, 不组成 BEM -->
</button>
```

## ❌ 违规示例

```vue
<!-- OssUploader.vue -->
<div class="oss-uploader">
  <input class="oss-uploader__input" />  <!-- ❌ 'oss-uploader__input' 包含 'oss-uploader' -->
  <div class="oss-uploader-trigger" />   <!-- ❌ 同上 -->
  <div class="ossUploaderInput" />       <!-- ❌ camelCase 仍包含子串 -->
</div>
```

```html
<section id="user">
  <div id="user-list"></div>             <!-- ❌ 'user-list' 包含 'user' -->
</section>
```

```vue
<!-- ❌ element-of-element 前缀子串 -->
<button class="result-cta__btn">
  <i class="result-cta__btn-ico" />        <!-- ❌ '__btn' 是 '__btn-ico' 前缀 -->
  <span class="result-cta__btn-label" />   <!-- ❌ '__btn' 是 '__btn-label' 前缀 -->
</button>

<!-- ❌ 单复数前缀子串 -->
<ul class="paywall-modal__features">
  <li class="paywall-modal__feature" />   <!-- ❌ '__feature' 是 '__features' 前缀 -->
</ul>
```

## ✅ 合规示例

```vue
<!-- OssUploader.vue -->
<div class="oss-uploader">              <!-- 根 = kebab-case 组件名 -->
  <input class="oss-pick__input" />     <!-- 子用独立命名空间 -->
  <span class="oss-pick__hidden-trigger" />
</div>
```

```vue
<!-- EpisodeListField.vue -->
<div class="episode-list-field">        <!-- 根 -->
  <div class="drama-ep__list">          <!-- 子用 drama-ep 命名空间 -->
    <div class="drama-ep__item">
      <div class="drama-ep__chip">EP1</div>
      <i class="drama-ep__action is-danger" />
    </div>
  </div>
</div>
<!-- list / item / chip / action 互不为子串 ✓ -->
```

```vue
<!-- OriginDramaCreator.vue -->
<div class="origin-drama-creator">      <!-- 根 -->
  <div class="playlet-creator__panel">  <!-- 子用 playlet-creator 命名空间 -->
    <div class="playlet-creator__left" />
    <div class="playlet-creator__tips" />
  </div>
  <!-- panel / left / tips 互不为子串 ✓ -->
  <!-- playlet-creator 也没有作为独立 class 出现 (只作前缀), 符合规则 5 -->
</div>
```

## 为什么这么做

1. **根 class = 组件名**: DevTools 元素树一眼能定位组件归属, 等同于隐式 `data-component` 标签
2. **互不为子串**:
   - 工具搜索 `oss-uploader` 不会同时命中 `oss-uploader__input`, 避免误改
   - **IDE Find Usages / Sublime / VSCode 跨文件搜索 `.cta` 不会同时高亮 `.cta-ico` / `.cta-label`** (本规则的典型痛点, 见下方截图案例)
   - `[class*="oss-uploader"]` 属性选择器不会匹配子元素
   - CSS Reset / Tailwind / 编译插件按 prefix 处理时不会误伤
3. **子元素独立命名空间**:
   - 让子元素的 BEM block 有独立语义 (`drama-ep` 比 `episode-list-field__item` 更聚焦"剧集"语义)
   - 重构根组件时不需要级联改子元素 class
   - 子元素 block 可在多个相邻组件复用 (前提: 保持全局唯一)
4. **扁平化 element**:
   - BEM 标准本身就推荐扁平 element, 不建议 `__a-b-c` 这种"祖父-父-子"链式命名
   - 避免重构时一个 element 改名引发 N 个相关 element 跟着改

### 真实案例: 2026-05 fe-picpopop StartBlurMask.vue 截图问题

```less
.blur-curtain__cta { ... }
.blur-curtain__cta-ico { ... }   // ❌ '__cta' 是 '__cta-ico' 前缀
.blur-curtain__cta-label { ... } // ❌ '__cta' 是 '__cta-label' 前缀
```

IDE Find in Files 搜索 `.blur-curtain__cta` 会同时高亮 5 个匹配 (`__cta` × 2 + `__cta-ico` × 2 + `__cta-label` × 1), 想精确定位某一个时极其干扰; 用 `:global([class*="blur-curtain__cta"])` 选择器时也会误命中 ico/label。修复后:

```less
.blur-curtain__cta { ... }       // ✅ 独立 element 名
.blur-curtain__icon { ... }      // ✅ 独立 element 名
.blur-curtain__label { ... }     // ✅ 独立 element 名
// cta / icon / label 三者两两互不为子串
```

## 与 LESS 嵌套规则联动

- ✅ 允许: `&.is-active` / `&:hover` / `&[disabled]` / `&::before` (伪类与状态类组合)
- ❌ 禁止: `&__item` / `&-success` / `&--large` (任何形式的类名字符串拼接)
- ❌ 禁止: 根 class 与子元素 class 互为子串 (本规则)
- ❌ 禁止: 同一 block 的两个 element 名互为前缀子串 (本规则规则 4)

## 自检命令

### 1. 根 class 与子元素子串关系 (替换 `<root>` 为根 class 名)

```bash
# class 属性中
rg --pcre2 'class="[^"]*\b<root>[\-_a-zA-Z0-9]+' src/

# .vue scoped style 中
rg '\.<root>[\-_]' src/
```

### 2. 跨文件搜索新 namespace 是否冲突 (避免重名)

```bash
rg '\.<new-namespace>[\-_\s\{,:]' src/
```

### 3. BEM element 间前缀子串扫描 (规则 4 自检)

针对单个文件, 抽取所有 `__xxx` element, 两两比对前缀子串:

```bash
# 抽取本文件所有 BEM element 名 (block__element 形式)
rg -o '\.[a-z][a-z0-9-]*__[a-z][a-z0-9-]*' <file> \
  | sort -u \
  | awk -F'__' '{print $2}' \
  | sort -u

# 然后人工/脚本两两比对 case "$a" in *"$b"*) FAIL ;; esac
```

或用一行 zsh 脚本:

```bash
rg -o '__[a-z][a-z0-9-]*' <file> | sort -u | while read a; do
  rg -o '__[a-z][a-z0-9-]*' <file> | sort -u | while read b; do
    [[ "$a" != "$b" ]] && case "$a" in *"$b"*) echo "VIOLATION: $a contains $b in <file>" ;; esac
  done
done
```

### 4. 单复数陷阱速查

```bash
# 找一个文件里既有 __xxx 又有 __xxxs / __xxx-list 容器的情况
rg -o '__[a-z][a-z0-9-]*' <file> | sort -u
# 人眼瞄一下相邻 element 名是否互为前缀
```

## 例外

- **公共工具类**: `.is-active` / `.with-affix-header` / `.do-form-section` 等不参与本规则
- **三方组件库**: element-ui、ant-design 输出的 class 不受约束
- **BEM modifier**: `__btn` 与 `__btn--again` / `__btn--download` 是 BEM 标准 modifier 关系, **不算违规** (`--` 后是同 element 的形态修饰, 不是新 element)
- **scoped 默认能力**: Vue scoped style 编译后会附加属性选择器, 本规则约束的是源代码可读层级
