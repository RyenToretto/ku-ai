# 设计稿还原开发流程

当存在 UI 设计稿时，**必须**严格遵循以下三阶段流程，禁止跳过任何步骤。

## 阶段一：设计分析（先文档，后编码）

1. 定位设计稿文件：在 `docs/**/**/` 或用户提供的截图中找到目标页面,如果有需要找到，对应的截图或者links、preview
2. 用 Chrome DevTools MCP 截取当前实现的截图（emulate 对应设备后 take_screenshot）
3. 逐区域像素级对比设计稿与当前实现，输出分析文档到docs/**/**/xxxx.md
4. 分析文档必须包含：
   - 逐区域属性对比表（圆角、高度、颜色、字号、间距等精确数值）
   - 差异状态标记（✅ 一致 / ❌ 不一致）
   - 修复清单（按优先级 P0-P2 排序）

## 阶段二：逐项修复

1. 严格按照分析文档中的修复清单逐项修改
2. 每个属性修改后用 Chrome DevTools `evaluate_script` 验证 computed style 数值
3. 禁止凭感觉估算数值，所有数值必须从设计稿量取或通过比例推算

## 阶段三：截图验证

1. 修复完成后用 Chrome DevTools 截图，与设计稿并排对比
2. 确认所有修复清单项均已标记 ✅
3. 有残余差异则回到阶段二继续修复

## 设计稿数值提取规则

- 圆角(border-radius)：对比同页面已知圆角元素推算，切勿把圆角方形识别成了50%的圆角
- 颜色：从设计工具或截图取色，RGB 精确到个位
- 间距/尺寸：基于 390px 视口宽度按比例换算
- 字号/字重：从设计标注或视觉比例推算

## 禁止事项

- ❌ 不分析设计稿直接编码
- ❌ 凭感觉写 CSS 数值
- ❌ 修改后不验证就提交
- ❌ 忽略 1-2px 级别的圆角/间距差异
