# next-steps-summary.mdc 设计说明

## 设计意图

同步自 `/Users/koujianfeng/.cursor/rules/next-steps-summary.mdc`，用于沉淀 Cursor Rule 的长期使用约束。

## 适用场景

- Cursor / Agent 在相关代码或工作流中自动参考
- 团队成员需要了解该规则背后的约束和使用方式

## 使用方式

将 `next-steps-summary.mdc` 放入目标项目的 `.cursor/rules/`，或作为全局规则放入 `~/.cursor/rules/`。
